<div class="message mb-2 <%= 'me' if message.user == current_user %>" id="<%= message.id %>">
    <div class="content-container">
        <div class="content">
            <%= message.content %>
        </div>
        <div class="time">
            <%= remove_zero message.created_at.to_s(:time_jp) %>
        </div>
        <div class="author">
            <%= message.user.username %>　
        </div>
    </div>
</div>